{
  "extends": "next",
  "rules": {
    "react/no-unescaped-entities": "off",
    "@next/next/no-page-custom-font": "off",
    "@next/next/no-img-element": "off",  // Disable warning for using <img> element
    "react/jsx-key": "error"  // Ensure that 'key' prop is used in lists (set to 'error' to enforce the rule)
  }
}
